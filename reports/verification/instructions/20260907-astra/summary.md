# Astra 持久化指令审计（2026-09-07）

## 依据与范围

已完整阅读 [OpenAI GPT-6 Astra Model Guidance](https://developers.openai.com/api/docs/guides/latest-model) 正文及 [Eric Provencher 原文](https://x.com/pvncher/status/2095991462416490862)（通过浏览器读取 X 文章全文）。前者建议审计冲突指令、校准测试范围与自主推进；后者强调精确的技能触发、按需加载，以及用决策边界替代过细流程。本次将这些建议用于开发助手指令，未迁移 Oria 的业务模型。

## 实际来源核对

| 来源 | 生效方式与处理 |
| --- | --- |
| 用户级 `~/.codex/AGENTS.md` | 与本会话注入的全局指令一致；已完整审阅。已有范围授权、按需阅读、复用证据和停止重复验证规则，保留。 |
| 项目 `AGENTS.md` | 本项目常驻入口；已修改。上级路径未找到其他 AGENTS/override，仓库含忽略文件的限定扫描未发现嵌套 AGENTS、项目 SKILL 或其他助手指令文件。 |
| 本地架构与详细路线 | 被项目入口引用，读取后影响执行；同步修改通用执行规则。两文件已被 Git 忽略，本次本地修改不会出现在普通 Git diff。 |
| 用户级配置 | 仅投影检查模型与指令相关配置键；默认模型为 `gpt-6-astra`，未发现自定义指令文件或文档回退配置；项目无 `.codex/config.toml`。 |
| 当前技能目录 | 核对本会话列出的 10 个技能描述；完整阅读本次触发的 openai-docs、context-token-optimizer，以及前者的一份引用。其他技能仅审阅触发描述，不将未加载正文误报为常驻指令。仓库无项目技能可修改。 |
| 平台与插件规则 | 由应用注入或共享安装，不属于项目文件，本次未改动。 |
| `src/oria/prompts/` | Oria 业务模型的版本化提示词，不是开发助手指令；不作 Astra 提示词迁移。 |

共享技能中 openai-docs 的触发描述范围较广，正文含重复的检索顺序约束；imagegen、plugin-creator 的描述也偏长，可在全局维护时进一步缩短。context-token-optimizer 与 find-skills 已是短路由；Deep research 明确要求用户触发。以上为描述/已读正文的静态审计，未做各技能行为评测。修改共享安装会影响其他项目，不在本次项目修改范围内。

## 修改结论

- 删除通用命名示例、假设性语言规范、早期提交历史；命令细节与 marker 改指向既有事实来源。
- 将全套文档阅读、逐任务基线重跑和通用检查清单改为任务范围、适用条件和停止条件；纯文字维护不套用版本实现流程。
- 常规实现选择交给模型，实质架构决策仍记录 ADR；修正从 V0.1-T01 重新起步的过时交接。
- 保留锁定工具链、安全授权、脱敏、阶段依赖、回归测试、CI 必需检查、冻结评测及 F/C/L/E-like/E 分层证据要求。
- 未发现项目自定义的强制委派或强制联网检索规则，不新增代理数量或搜索轮数要求。

## 验证与恢复

修改前工作区干净。原文备份在本机 `/private/tmp/oria-astra-instructions-20260907/`；该目录包含三份原文件，可逐文件恢复，临时目录不保证长期保存。

已检查三份文件的逐行差异、UTF-8/LF/末尾换行、引用目标及残余通用强制规则；`git diff --check` 通过。没有运行 Python 测试、构建、Live 或企业验证；未更改业务阶段完成状态。

AGENTS.md：1752 → 1389 个 Unicode 字符，减少 363（20.7%）；不是实测 Token 节省。详细路线因补充适用边界略增字数，主要收益是减少无关读取和重复执行。未进行模型 A/B 或费用测试，当前会话已加载内容不会因文件修改立即减少。
